# Agent Fan-out Journal

Append-only ledger of autonomous agent sessions running the v2 (and later) idempotent fan-out prompt. One row per session, written by the session itself at Phase 4 receipt time. Read this file first when starting a new fan-out session to avoid duplicating recently completed work.

Columns: `timestamp_utc | session_id | agent_family | phase_id | pr_number | outcome`

`outcome` ∈ {`shipped`, `finish-existing`, `deferred`, `no-work`, `conflict`}

---

2026-05-17T16:56:00Z | droid-DC5A5821 | droid | P03-lane-registry-claim-helper-rebase | 7267 | finish-existing

2026-05-17T17:23:00Z | droid-A5312D6A | droid | P05-publication-freshness-probe-rebase | 7261 | finish-existing
2026-05-17T17:23:00Z | droid-A5312D6A | prompt-bug: v3 heredoc shim '/tmp/fanout_claim.py <<PYEOF ... PYEOF' still hangs in test shell; v4 must ship shim as tracked scripts/ file (see P12 deferral)
2026-05-17T20:53:00Z | droid-6916BE6B | droid | P02-freshness-probe-rerun | 7287 | shipped
2026-05-17T20:53:00Z | droid-6916BE6B | prompt-bug: v4 lists scripts/triage_open_prs.py as required observer but doesn't say what to do when it isn't yet on main (it's still in PR #7285); fell back to manual bucket classification against OPERATOR_DELEGATION_POLICY.md
2026-05-17T23:25:00Z | droid-826081D8 | droid | P13a-canonical-km-adapter-count-drift | 7289 | shipped
2026-05-17T23:25:00Z | droid-826081D8 | prompt-bug: v5 lists scripts/detect_active_lane_collisions.py as a required observer; that script does not exist on main — collision detection landed in scripts/agent_bridge.py health (#7288). v6 should reference agent_bridge.py health instead.
2026-05-18T02:21:00Z | droid-F46C5B20 | droid | P17-stage3-triage-bucket-c-batcher | 7294 | shipped
2026-05-18T02:21:00Z | droid-F46C5B20 | note: v7 prompt was the cleanest iteration yet — no required-observer drift, no hung-shim issues, no missing-flag issues. Two minor v8 polish items: (a) canonicalize the y-advance label name (rollout doc says "label + ready + comment" but doesn't pin a name; v7 P17 shipped without the label step to avoid creating a phantom label), and (b) decide whether to keep YAML-as-JSON-superset response files or add an opt-in PyYAML loader.
2026-05-18T04:33:00Z | droid-F473CDBF | droid | P20-model-pins-frontier-aligned | 7306 | shipped
2026-05-18T04:33:00Z | droid-F473CDBF | note: v8 prompt was clean to execute (no hard prompt-bugs). Two minor accuracy notes for v9: (a) v8 ack list said model_pins.frontier_aligned was no longer failing; it WAS still failing — P20 closed it. (b) v8 P23 (km_adapters drift) was already fixed by #7289 P13a; canonical-metrics receipt shows it passes. v9 should refresh both facts from the live canonical-metrics receipt at prompt-publication time, and clarify P19's "owner detection" since `gh pr view --json author` returns the operator's GitHub login (an0mium) for any PR opened via `gh` on the operator's machine — lane ownership lives in the **branch name prefix**, not the author field.
2026-05-18T04:50:00Z | claude-79AAF84B | claude | P28-A-identify-lane-owner | 7308 | shipped
2026-05-18T04:50:00Z | claude-79AAF84B | prompt-bug: `agent_bridge.py lanes --json` CLI view returned 8 records while raw `.aragora/agent-bridge/lanes.json` had 12 (including the 4 active ones). Codex session ran into the same surfacing-discrepancy this session ("I cannot find the owner" — owner WAS in the raw file). Phase A consolidator reads the raw file directly to dodge this; v9 should call this out as a known CLI-vs-file mismatch until upstream is fixed.
2026-05-18T04:50:00Z | claude-79AAF84B | note: P28-A ships Phase A only (read-only consolidator). Phases B (mailbox writer), C (operator-snapshot extension to surface pending messages), D (docs PR), E (claim-helper env-var auto-populate) tracked as follow-on PRs. Schema is forward-compatible; Phase B can start writing files without changing what A surfaces.
2026-05-18T04:55:00Z | claude-E43E46C9 | claude | P24-canonical-test-definitions-count-drift | 7307 | shipped
2026-05-18T04:55:00Z | claude-E43E46C9 | prompt-bug: v8 P24 instruction misdiagnosed the drift as stale-docs and recommended lowering the claim. Actual root cause was a counter bug — `_observe_test_definitions_count` regex `r'^\s*def test_'` excluded `async def test_` and undercounted by ~27%. Applying honesty rule H2 (raise claim to match production / fix the counter, don't lower to match undercount) was the right move. v9 P24-style instructions should prompt agents to verify the counting method matches METRICS.md's documented methodology before assuming the claim is stale.
2026-05-18T04:55:00Z | claude-E43E46C9 | note: shipped at zero failures per operator's wakeup directive despite 1 CANCELLED (`build`, not from force-push) + 1 pending (`Baseline Determinism`). Canonical-metrics went 8 pass / 1 fail / 1 warn → 9 pass / 1 fail / 0 warn. CLAUDE.md retains stale 216,000+ refs (protected file, not edited) — within tolerance of the corrected counter anyway.
2026-05-18T05:27:40Z | codex-6B2B5435 | codex | P28-refresh-worktree-value-inventory | - | shipped
2026-05-18T05:13:00Z | droid-3D81079C | droid | P28-worktree-inventory-refresh | - | shipped
2026-05-18T05:13:00Z | droid-3D81079C | note: v9 P28 (worktree-inventory-refresh) refreshed docs/status/generated/worktree_value_inventory/latest.json (24.9 h → 0 h). Diff vs prior: cleanup_candidates 5→7, harvest_candidates 28→34, preserve 12→15, total 45→56. Constrained scan args (`--root .worktrees/codex-auto --skip-gh --size-mode none --git-timeout 5`) — default scan over-traverses on this machine and doesn't fit a 10-min budget.
2026-05-18T05:13:00Z | droid-3D81079C | prompt-bug: v9 P28 lane-id ("P28-worktree-inventory-refresh") collides with claude-79AAF84B's already-shipped "P28-A-identify-lane-owner" — different concerns sharing a number. v10 should renumber the worktree-inventory phase (suggest P40) and leave P28-A/B/C/D for the lane-owner family. Also: v10 P24 should be RETIRED (claude-E43E46C9 shipped #7307), and v10 should diff the journal vs ack list at Phase 0 to prune phases that landed during prompt drafting.
2026-05-18T05:18:00Z | droid-3D81079C | prompt-bug: same-work, different-lane-id collision detector blind spot. While droid-3D81079C ran lane `P28-worktree-inventory-refresh` (claimed 05:03:30Z), codex-6B2B5435 claimed `P28-refresh-worktree-value-inventory` at 05:08:53Z — same job, near-duplicate name, both targeting `docs/status/generated/worktree_value_inventory/latest.json`. `agent_bridge.py health` reports 0 collisions because the literal lane_ids differ. v10 fix options: (a) require canonical lane_id schema `P<num>-<verb>-<noun>` and reject reorderings at claim time; (b) make collision detector normalize by token-set hashing or by canonical phase name; (c) add a Phase 2 pre-claim rule "if any active lane SHARES the phase-num prefix AND mentions the same primary noun (e.g. 'worktree-value-inventory'), require coordination". Symptom: idempotent publisher tolerates the double-run (last-writer-wins, snapshot artifact is dated), but the registry is misleading and journal readers see two parallel rows.
2026-05-18T05:26:38Z | codex-9FB91BD7 | codex | P32-clean-legacy-top-level-worktrees | - | shipped
2026-05-18T05:20:00Z | claude-86616832 | claude | P29-steering-mailbox-writer | 7310 | shipped
2026-05-18T05:20:00Z | claude-86616832 | prompt-bug: lane renumbered mid-session from P28-B → P29 because the prompt's "if ANY P28-* lane shows active → stop" rule kept firing on droid-3D81079C / codex-6B2B5435's worktree-inventory lanes (both `status=active` despite their `shipped` journal rows — they never called `--status completed`). Two v10 fixes: (a) operator-side: canonical numbering plan that pins each primitive's number range (P28-A/B/C/D/E reserved for agent-steering, P40-* for worktree-inventory, etc.) — see droid row 31's matching suggestion; (b) lane-registry hygiene: a sweeper that reconciles journal `shipped` rows against stuck `active` lanes and auto-transitions them, OR a TTL on `active` lanes that expires to `stale` after N hours of `updated_at` silence. Without (b), every future fan-out session that uses the strict "any active P<n>-* → stop" rule will hit the same renumbering churn.
2026-05-18T05:20:00Z | claude-86616832 | note: shipped Phase B of the agent-steering primitive. End-to-end loop now write-side complete: send_operator_steering.py (this PR) writes; identify_lane_owner.py from PR #7308 (Phase A) reads via pending_message_count. Live smoke confirmed pending_message_count went 0→1 across the boundary. Phase C (operator-snapshot extension to surface pending in agent_bridge snapshot output) is the next bounded follow-on; tracked but unclaimed.
2026-05-18T05:45:39Z | codex-5B032BF7 | codex | P45-legacy-worktree-dir-audit-clean | - | shipped
2026-05-18T05:45:00Z | claude-7E0F63B3 | claude | P30-operator-snapshot-steering-messages | 7311 | shipped
2026-05-18T05:45:00Z | claude-7E0F63B3 | note: Phase C of agent-steering primitive complete. operator-snapshot now surfaces pending_steering_messages as additive top-level field; scoped via env ARAGORA_SESSION_ID or --steering-recipient flag, else operator rollup. End-to-end A→B→C smoke confirmed live (count=1 across the boundary). All 8 pre-Phase-C top-level keys + 11 summary subkeys preserved (regression-tested). _acked/ subdir excluded per Factory's Phase D convention (hardwired here, documented in Phase D when it ships). Phase D (docs + ack convention) + Phase E (claim-helper env-var auto-populate) tracked unclaimed.
2026-05-18T05:45:00Z | claude-7E0F63B3 | prompt-bug: Factory's Prompt 1 specified test #7 as "operator-snapshot --json regression — existing top-level keys (timestamp, lane_conflicts, process_census, health, summary, records_omitted) still present". The actual current shape has 8 top-level keys (broker_runs, health, lane_conflicts, lanes, process_census, sessions, summary, timestamp) — `records_omitted` only appears in --summary-only output, NOT default. Implemented the regression test against the actual default-mode shape; if Factory's intended shape was --summary-only, that's a separate test addition for Phase D / v10. Symptom: zero functional impact, but v10 fan-out prompt-bug check should diff documented-spec vs actual-shape before publishing prompt.
2026-05-18T06:12:30Z | codex-7FFAEAF0 | codex | P22-check-canonical-metrics-json-flag | 7313 | shipped
2026-05-18T12:14:19Z | codex-2D173797 | codex | P29-cleanup-safe-removable-worktrees | - | shipped
2026-05-18T12:31:27Z | codex-D02A76F9 | codex | P47-operator-snapshot-active-lane-parity | 7324 | shipped
2026-05-18T12:40:21Z | codex-9B0801A8 | codex | P48-settle-7324-operator-snapshot-parity | 7324 | shipped
2026-05-18T13:03:57Z | codex-29324B8A | codex | P49-final-watch-7324-operator-snapshot-parity | 7324 | shipped
2026-05-18T17:15:00Z | droid-4DDC6773 | droid | P50-smart-harvest-rerun-and-cleanup | - | shipped
2026-05-18T17:15:00Z | droid-4DDC6773 | note: v11 P50 (renumbered from v11's P47 per R18 to avoid collision with codex #7324 P47). Ran inventory with new --smart-merge-detection flag (P42 #7312 just-shipped). Removed 2 confirmed-removable post-merge worktrees: droid-20260518-041442-0e8aade8 (464 MB, post-#7306 merge) + codex-work-owner-lane-enrichment-20260518 (430 MB, post-#7309 merge). Total recovered: **894 MB**. Inventory diff: total 56→44, cleanup 7→4, harvest 34→25, preserve 15 (unchanged). Deferred 3 dirty no_git_cache_residue wrappers (D2 + inspect false-positive on empty nested wrappers). New snapshot: `worktree-value-inventory-20260518T170310Z.json`.
2026-05-18T17:15:00Z | droid-4DDC6773 | prompt-bug: `--smart-merge-detection` is hobbled by `--skip-gh`. The classifier can't recognize OPEN-PR branches without gh access, so it misclassifies them as harvest_candidates. Multiple verified false positives in the current 25-item harvest list: worktree-packets-keyboard-throughput (#7278 open), claude/P24/P28-A/P29/P30 (#7307/#7308/#7310/#7311 open), droid/P16-stage2 (#7292 LINCHPIN, 12 commits ahead — should never be harvest!), droid/P02-freshness-probe-rerun (#7287 merged), droid/P17-stage3 (#7294 open). v12 fix options: (a) recommend `--include-pr-state` flag that supplements --skip-gh with cached `gh pr list --state open --json headRefName` at scan start, (b) auto-merge the basic check + smart check + open-PR check into a single classification step.
2026-05-18T17:15:00Z | droid-4DDC6773 | prompt-bug: safe_worktree_cleanup.py inspect over-flags `dirty=True` for empty nested-worktree wrapper dirs (4-32 KB residue with only an empty .worktrees/ subdir). D2 conservatism blocks removal even though the contents are provably empty. v12 prompt-bug item: add `--ignore-empty-nested-wrappers` flag, or have inspect recognize the empty-wrapper sentinel pattern. Currently 3 such dirs in `.worktrees/codex-auto/` immortal-deferred.
2026-05-18T17:10:00Z | codex-8C79E182 | codex | P55-settle-agent-steering-primitive-chain | 7310/7311 | finish-existing
2026-05-18T17:20:00Z | claude-B061F80D | claude | P54-agent-dispatch-reach-plan | 7327 | shipped
2026-05-18T17:20:00Z | claude-B061F80D | note: docs-only plan PR. Adds `docs/governance/AGENT_DISPATCH_REACH_PLAN.md` (5-phase, ~240-line plan) closing four "weak/does-not-exist-as-advertised" gaps: Droid CLI reach, Codex Desktop reach, universal dispatch-to-all, one-button wake-up. Phase 1 adds `contact_method` to `LaneRecord` (additive); Phase 2 ships `scripts/wake_agent.sh`; Phase 3 osascript Codex Desktop bridge; Phase 4a/4b Droid local-tmux + mailbox sidecar; Phase 5 bootstrap sweeper. PR draft, no labels, no merges. Implementation lanes follow only after operator + non-Claude adversarial review.
2026-05-18T17:20:00Z | claude-B061F80D | note: in-session worker dispatch — spawned claude-p52 (Phase D agent-steering primitive docs) and claude-p53 (Phase E claim-helper env-var auto-populate) via `agent_bridge.py launch` autonomous mode. claude-p52 correctly deferred to await Phase A/B/C merges to main (#7308/#7310/#7311) — same conclusion as codex-8C79E182's P55 receipt. claude-p53 proceeded; independent of upstream merge state.
2026-05-18T17:25:00Z | claude-1DA627A6 | claude | P53-claim-helper-env-var-auto-populate | 7328 | shipped
2026-05-18T17:25:00Z | claude-1DA627A6 | note: Phase E of the agent-steering primitive — single-file additive edit to `scripts/claim_active_agent_lane.py` adding `_identity_fields_from_env()` that auto-populates `codex_thread_id` / `codex_rollout_path` / `session_title` / `desktop_label` from canonical env vars (`CODEX_THREAD_ID`, `CODEX_ROLLOUT_PATH`, `CLAUDE_SESSION_ID`, `FACTORY_DROID_SESSION`) when the matching CLI flags are absent. ADDITIVE only; CLI flags retain precedence. No schema change, no new imports, no atomic-write change. Tests extend the existing module (28 → 37 passing). Smoke confirmed via tmp registry. Draft PR #7328, no labels, no merges.
2026-05-18T17:25:00Z | claude-1DA627A6 | note: `ARAGORA_SESSION_ID` intentionally has no env fallback — it is the operator-facing primary identifier already passed via the required `--owner-session` flag; a silent default would defeat the explicit-claim contract. If `FACTORY_DROID_SESSION` is not the actual env-var name Factory Droid CLI sets, the helper's mapping should be renamed in a follow-up CL (logged here as a v11/v12 prompt-bug candidate so future me/operator can sanity-check the constant against a real droid session env dump).
2026-05-18T17:34:00Z | droid-D2D761BC | droid | P60-stale-local-branch-sweep | - | shipped
2026-05-18T17:34:00Z | droid-D2D761BC | note: v12 P60. Deleted 889 local branches via 3 filter union: gone-upstream (889 hits) + shipped-lane-id-match (10, mostly dedup) + 0-unique-commits-vs-main (2). 2202→1313 branches. `.git` 2.7G→2.6G after `git reflog expire --expire=now --all && git gc --prune=now`. Excluded 123 worktree-bound branches + main. Most remaining 1313 have squash-merged unique commits invisible to lineage-based filters; reaching the v12 spec's "≤200" target would require a `git cherry` patch-equivalence pre-pass per branch.
2026-05-18T17:34:00Z | droid-D2D761BC | prompt-bug: v12 P60 acceptance target "≤200 local branches" is unrealistic without per-branch patch-equivalence detection. Squash-merge breaks lineage so `--merged` + `rev-list --count = 0` miss the work. v13 should either (a) loosen the threshold (e.g., ≤ before×0.5), or (b) ship a "git cherry against origin/main" pre-pass and only delete branches with all unique commits patch-equivalent.
2026-05-18T17:45:00Z | droid-88A43575 | droid | P59-inspect-empty-wrapper-detection | - | shipped
2026-05-18T17:45:00Z | droid-88A43575 | note: v12 P59 — pivoted from "fix detection" to "defense-in-depth + regression test + cleanup" since `git status --short` empty-stdout behavior already produces `dirty=False` for these dirs. Added `_is_empty_nested_wrapper()` fast-path to `_worktree_is_dirty()` (frozenset of sentinel filenames: .claude-session-anchor, .codex-session-anchor, .codex-session, .droid-session-anchor, .session-anchor; requires `.worktrees/` subdir). +2 regression tests (positive + negative). 16/16 passing. Purged the 3 immortal-deferred wrapper dirs (60 KB total): claude-20260501-195315-89c3c0ad, codex-20260422-194119-a5c0dd59, rbac-health-fix-20260413-1.
2026-05-18T17:50:00Z | droid-56778BD7 | droid | P58-inventory-classifier-include-pr-state | - | shipped
2026-05-18T17:50:00Z | droid-56778BD7 | note: v12 P58 — added `--include-pr-state` flag to codex_worktree_value_inventory.py. Single bulk `gh pr list --state open --limit 500 --json number,title,url,headRefName` call at scan start populates an in-memory cache of `headRefName -> open PRs` for the InventoryContext; `lookup_open_prs()` consults the cache without subprocess when populated. CLI flag is opt-in (default off; legacy behavior preserved). Live test: 7 of 25 harvest_candidates reclassified to `open_pr_or_outbox` (preserve) — exceeds spec ≥7 acceptance. +47 LoC script, +88 LoC tests, 37/37 passing. Published snapshot `docs/status/inventories/codex_worktree_value_20260518T174656Z_droid-56778BD7_include-pr-state.json`.
2026-05-18T18:00:00Z | droid-D602B3C0 | droid | P63-lane-registry-staleness-sweeper | - | shipped
2026-05-18T18:00:00Z | droid-D602B3C0 | note: v12 P63 — new `scripts/sweep_stale_lane_claims.py` detects stale active lane claims via 3 signals (branch_missing, worktree_missing, stale_updated_at) with `--branch-grace-hours` (default 1h) guarding fresh claims. Default `--dry-run` (read-only); `--apply` rewrites stale rows in place with `status=expired conflict_reason="stale: <reasons>"`. Atomic rewrite; never deletes rows. +290 LoC script, +253 LoC tests, 9/9 passing. Live dry-run against current 51-row registry finds 0 stale (all 4 active claims are recent and within grace period).
2026-05-18T18:08:00Z | droid-8690DFC5 | droid | P64-collision-detector-token-normalize | - | shipped
2026-05-18T18:08:00Z | droid-8690DFC5 | note: v12 P64 — added `_normalize_branch_token` and `_normalize_worktree_token` helpers to `scripts/claim_active_agent_lane.py`. Lane collision detection now recognizes equivalent identity tokens across `refs/heads/` / `origin/` prefixes, trailing slashes, and macOS symlink-resolved paths (e.g. /tmp ↔ /private/tmp). +19 LoC script, +82 LoC tests, 34/34 passing.
2026-05-18T18:11:30Z | droid-76CB27A3 | droid | P61-62-remote-orphan-sweeps | - | shipped
2026-05-18T18:11:30Z | droid-76CB27A3 | note: v12 P61+P62 — bulk-deleted 50 remote-only orphan branches: 31 in `benchmark-truth-publication/*` (retired CI snapshot workflow) and 19 in `codex-automation/fix-*` (codex automation fix bot). Verified 0 overlap with 32 currently-open PRs via `gh pr list --state open --limit 500`. Single `git push origin --delete` call printed 50 `[deleted]` lines. Remote branch count: 425 → 375 (-50). Idempotent.
2026-05-18T18:20:00Z | droid-CCCE0130 | droid | P65-swarm-micro-worktree-triage | - | shipped
2026-05-18T18:20:00Z | droid-CCCE0130 | note: v12 P65 — aligned `has_active_session()` in `codex_worktree_value_inventory.py` with the canonical implementation in `codex_worktree_autopilot.py`. `.claude-session-anchor` is a passive wrapper sentinel, not an active session lock; the inventory was the outlier. Also widened `_is_empty_nested_wrapper()` in `safe_worktree_cleanup.py` to recognize flat anchor-only wrappers (only sentinel file at top level, no `.worktrees/` subdir). 16 of 17 flagged wrappers purged after the fix. Inventory `active_or_dirty: 16 → 2`; `cleanup_candidate_count: 1 → 5`. Tests 38/38 + 17/17 passing.
2026-05-18T18:28:00Z | droid-9383F0AA | droid | Q07-clean-pr-automerge-gap | - | shipped (findings)
2026-05-18T18:28:00Z | droid-9383F0AA | note: v12 Q07 — forensic root-cause for #7278+#7268 not auto-merging despite CLEAN mergeStateStatus. Both PRs are still drafts (isDraft=True). `merge_codex_automation_prs.py` explicitly skips drafts. PR #7268 (codex/...) would clear the namespace gate after ready-transition + validation-evidence body string; PR #7278 (worktree-...) is outside the codex/* namespace filter and would need manual merge or v13 extension to handle worktree-* branches. No state mutation performed (other author's drafts).
2026-05-18T18:30:00Z | droid-2D9AB288 | droid | Q06-obsolete-blocked-pr-close-sweep | - | shipped (no-op)
2026-05-18T18:30:00Z | droid-2D9AB288 | note: v12 Q06 — surveyed all 33 currently-open PRs. Age >7d: 0, >14d: 0, >30d: 0. mergeStateStatus: 31 UNKNOWN + 2 CLEAN. Zero PRs match the obsolete-BLOCKED close criterion. PR hygiene is fresh — likely a consequence of the fast-moving recent fan-out cycles. No closures performed.
2026-05-18T19:32:10Z | droid-C4A021F3 | droid | P71-mypy-preempt-helper | - | shipped
2026-05-18T19:32:10Z | droid-C4A021F3 | note: v13 P71 — new `scripts/preflight_mypy.sh` (POSIX-friendly, macOS bash 3.2 compatible) runs `mypy --pretty` against `git diff --name-only <base>...HEAD -- '*.py'` using the repo's existing `[tool.mypy]` config in pyproject.toml (no config changes, no new deps, no CI changes). Supports `--diff-base <ref>` (default `origin/main`). Skips with "no python changes; mypy preflight skipped" and exit 0 when the set is empty; otherwise exits with mypy's exit code and prints a remediation hint. Added `docs/dev/MYPY_PREFLIGHT.md` with rationale + Phase 2 integration snippet. Smoke 1 (no diff vs origin/main): exit 0 with skip message. Smoke 2 (`_tmp_mypy_smoke.py` with 2 deliberate errors, committed to lane HEAD, then reverted): mypy surfaced both `[return-value]` and `[arg-type]` errors and exit 1. Receipt: `docs/status/P71_MYPY_PREEMPT_RECEIPT_20260518T193210Z.md`. shellcheck not installed in this env (per spec, "if installed").
